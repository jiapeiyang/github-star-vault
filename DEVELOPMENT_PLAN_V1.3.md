# Star Vault v1.3：找得准、便于比较与持续维护

2026-09-07，用户已批准按建议开发。基线 c5a1c43（v1.2 已发布）。本轮先完成本地开发与验收，发布另按对应授权执行。视觉与事实/人工资料分离沿用现有约定，不恢复学习系统。

## 本轮范围

1. 搜索：默认有词时按相关性、无词时按收藏时间；名称、标签和摘要优先于正文，保留显式时间/Stars排序；少量人工同义词，全部概念同时匹配，避免英文短词扩展误命中。
2. 浏览：仓库入口使用标准链接，普通点击保留单页浏览上下文，⌘/Ctrl/中键与右键保留浏览器行为；详情独立标题；搜索结果提供章节命中入口与正文高亮。
3. 比较：24个重点项目维护有来源的关联说明，保留旧关联；3个用途专题各8项，解释适用场景与差异。
4. 维护：将待补资料、来源待核查与资料受限清楚区分。提供标准库只读来源检查命令，记录README变动，不因普通提交就判定介绍过期，不自动改写正文或增加后台定时服务。
5. 示例：10个代表仓库补明确输入、操作与输出；在隔离环境验证适合离线运行的例子，注明具体版本和验证范围，其他示例保留未执行说明。

## 验收

- 冻结20个已收录仓库的检索用例，记录原排序与新排序；另用构造数据验证相关性优先级、别名边界、多词、显式排序与稳定顺序。
- 桌面与390px下验证搜索→命中章节→返回、详情标题、新标签页、专题→详情，以及维护状态和导出回读。
- 24条资料的关联有对应理由，专题ID/仓库ID、来源检查报告、状态字段有结构校验；保留资料导出新增元数据。
- 示例记录与实际运行日志一致，关闭本轮临时服务和浏览器。
- Python测试、前端测试、Sites测试、生产构建、git diff --check通过。

不引入数据库、语义搜索、在线编辑保存、自动分类、缓存层或学习状态。来源变化只是复核线索，未实际验证的例子不写成已实测；不把受限条目包装成可用工具。

## 本地完成记录

2026-09-07：五项实现均已完成。20 个冻结搜索用例达到目标，3 × 8 个专题和 24 份关联资料通过校验，10 个具体示例运行通过。287 份 README 已检查：284 不变、3 份变化进入复核队列。额外修复验收中发现的桌面列表滚动恢复问题。

Python 28、前端 32、Sites 4 个测试及 Pages 模式构建通过；桌面和 390px 验收有截图与回读记录。新页 URL 可独立访问；IAB 没有触发原生中键／Meta 新页，未将该项写为完整浏览器实测。细节、保留边界和证据见 [v1.3 本地交付](docs/10-v1.3-delivery.md)。用户随后已授权提交并通过现有 GitHub Pages 流水线发布。
