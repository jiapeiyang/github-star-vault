# Star Vault v1.3 本地交付

2026-09-07。基线 `c5a1c43`，开发分支 `codex/star-vault-v1.3`。本文记录本地开发与验收快照；用户随后授权提交并发布至现有 GitHub Pages，部署结果见 [Actions](https://github.com/jiapeiyang/github-star-vault/actions/workflows/site.yml)。

## 已实现

- 输入搜索词时默认按相关性排序，无词时按收藏时间。精确仓库名称优先，名称、标签和摘要高于正文；显式时间和 Stars 排序仍可使用。人工维护 8 组同义词，多概念继续同时匹配。
- 仓库和专题入口改为标准链接，详情有独立标题；搜索结果可定位对应二级章节，正文文本高亮而代码不改写。
- 视频创作、前端设计、Agent 工作流各 8 个项目。24 份重点资料共 73 条带说明的有方向关联，其中保留既有关联 1 条。
- 手动来源检查命令、README 版本报告和维护队列。区分待补介绍、来源待核查、资料受限；不根据上游普通代码提交推断解读过期。
- 10 份资料新增具体输入、操作与实际输出；交付完整复验脚本和独立依赖说明。原示例仍明确未执行。
- 修复桌面独立列表滚动位置丢失：详情返回与浏览器后退同时恢复列表区域和整页的位置。

没有恢复学习阶段、笔记、实践记录或完成数。

## 检索验收

先冻结 [20 个用例](../app/tests/search-cases.json)，再比较 v1.2 原始资料和默认收藏排序，与 v1.3 当前资料和默认相关性排序。所有目标都在两个版本实际收录；不会使用浏览过但未收藏的仓库作为目标。

全部 20 个达到各自的前 1／3／5 名标准。部分结果：

| 查询 | 目标 | v1.2 | v1.3 |
| --- | --- | ---: | ---: |
| koa | koajs/koa | 8 | 1 |
| herdr | herdrdev/herdr | 3 | 1 |
| archify | tt-a1i/archify | 3 | 1 |
| 文字识别 | opendatalab/MinerU | 未命中 | 1 |
| 截图 前端 | abi/screenshot-to-code | 6 | 1 |
| ppt 可编辑 | hugohe3/ppt-master | 3 | 2 |
| 智能体 教程 | datawhalechina/hello-agents | 1 | 2 |

这是固定样本验收，不代表所有查询都改善；最后一个用例也如实保留了排序下降。完整前五项见 [检索报告](audits/2026-09-07/v1.3-search-check.json)，执行器为 `app/scripts/evaluate-search.mjs`。基线目录由 `git archive c5a1c43` 的事实、资料、配置和旧构建模块生成，旧过滤器来自同一提交。

## 来源与内容验收

`check_guide_sources.py` 检查 287 个当前收藏：284 份 README 版本不变，3 份变化，0 份不可读取，0 份缺基线。变化条目为 `awesome-vpn/awesome-vpn`、`itgoyo/TelegramGroup`、`boyang-hu/website-rebuild-skill`。它们进入来源待核查；本次没有代替人工完成这三篇的新版本复核，也没有抹掉提示。

资料受限仍是 B站接口资料、网易云音乐接口、mac-env 这 3 个历史条目。它们与 README 变化是不同概念。全部 287 份文章通过当前内容契约；事实快照没有被内容维护改写。

- [24 个重点项目与专题来源](content-batches/v1.3-topics-report.md)
- [10 个具体示例与执行范围](content-batches/v1.3-examples-report.md)
- [完整脚本、版本与复验方法](examples/v1.3/README.md)

10 项执行最终退出 0、stderr 为空；交付的 11 个脚本（含统一入口）与实际执行目录逐文件一致。Koa 临时服务已关闭；Sequelize 不连接数据库，Solid 不渲染 DOM，Svelte 只验证服务端编译结果。node-xlsx 明确使用历史依赖组合，未声称验证最新版。

## 软件验证

- Python：28 个测试通过，含来源读取成功／变化／不可用、专题与关系校验、版本复核与旧报告失效。
- 前端：32 个测试通过，含相关性、同义词边界、URL/hash、链接修饰键判定、Markdown 安全高亮，以及新增元数据经真实 Python 解析器回读。
- Sites：4 个测试通过；GitHub Pages 模式生产构建通过，主 JS gzip 约 129.7 kB。
- `verify_data.py`：active=287，total=288，curations=287；`git diff --check` 通过。
- 浏览器：桌面 1280px 和手机 390px 下检查搜索、章节定位、正文、高亮、复制、专题与维护；相关页面没有横向溢出，控制台 error/warn 为 0。
- 真实滚动验收：桌面 `.library-feed` 滚动到 985px，打开 RxJS 详情，使用返回按钮和浏览器后退后分别恢复为 985px。生产模式 `/github-star-vault/` 路径的链接与章节定位通过。
- 资料受限条目的浏览器复制导出经过回读：所有源字段和正文保留，仅将省略的 `personal_archived` 显式写为等价的 false。

页面证据：[桌面搜索](audits/2026-09-07/v1.3-search-desktop.png)、[桌面专题](audits/2026-09-07/v1.3-topics-desktop.png)、[资料维护](audits/2026-09-07/v1.3-maintenance-desktop.png)、[手机搜索](audits/2026-09-07/v1.3-search-mobile.png)、[手机详情](audits/2026-09-07/v1.3-detail-mobile.png)、[手机专题](audits/2026-09-07/v1.3-topics-mobile.png)。

独立详情 URL 在另一个新页打开后，标题、查询和章节定位正确；没有返回上下文时能回到保留查询的项目库。IAB 的中键和 Meta 点击没有创建新页，原页也未跳转，因此这里不把浏览器原生快捷键触发或右键菜单写成实测通过。标准 href 与“只拦截普通主键点击”的逻辑已验证；原生 Chrome/Safari 快捷键仍属于未覆盖的人工验收边界。

## 我没有防什么

没有引入语义搜索、自动分类、后台来源巡检、缓存或重试队列。来源检查只比较默认 README，不检查全部文档和 API，更不保证示例永远兼容；3 份变更资料留待下一次实际复核。搜索验证是固定样本，未承诺任意自然语言都能找准。没有用 Node 小例子冒充浏览器、数据库或生产场景验证，也没有完成真机 Safari、原生开新页快捷键和未导出确认框的全部交互验收。

本轮临时浏览器页与 4180/4181 服务在验收后关闭，尺寸覆盖恢复。保留用户既有页面、工作文件和 `app/qa/production-learning.png` 的原有改动。
