# Star Vault v1.1 开发计划：整理与阅读闭环

> 制定日期：2026-09-05
> 状态：P1～P3、P5 已完成并上线；P4 的 30 个项目已按用户授权直接归类并建立正式文件。
> 前置版本：已发布的 v1 MVP，参考提交 `83c2bd5`。
> 优先级：本计划定义下一版新增范围；未明确修改的约束继续遵循 `DEVELOPMENT_PLAN.md` 和 `AGENTS.md`。

## 1. 版本目标

让用户能找到任意待整理项目，在浏览器填写或修改策展内容，下载可直接提交的文件，并在发布后完整阅读学习笔记。

```text
搜索或浏览待整理项目
→ 填写分类、价值说明与笔记
→ 预览并下载 <repo_id>.md
→ 放入 content/repos/，审阅 diff 并提交
→ Actions 校验并发布
→ 网站展示分类、学习阶段和完整笔记
→ 后续可从详情重新打开文件生成表单
```

交付分为两部分：软件功能完成；首批 30 个项目完成分类与正式导入。2026-09-05 用户明确授权助手自行整理归纳，取消分类逐项确认要求；个人学习与实践结论不根据分类推断。

## 2. 已核实的现状与缺口

以下依据本地代码和快照，不是实时 GitHub 数量承诺。实施前重新读取仓库状态。

| 项目 | 当前事实 | 本版要解决的问题 |
|---|---|---|
| 数据规模 | 当前快照 active 284、total 285，6 份策展文件 | 278 个项目仍未整理，需要首批分类工作 |
| 详情 | `DetailView.jsx` 只显示 note、takeaway、tags 等 | `contentMarkdown` 已存在但正文没有完整展示 |
| 收件箱 | `InboxView.jsx` 使用 `candidates.slice(0, 30)` | 第 31 条以后的项目没有列表入口 |
| 学习页 | queued、learned 使用 `slice(0, 8)` | 分组缺少查看全部入口，最近已学习没有显式按个人更新时间排序 |
| 整理 | 表单只支持 inbox/imported，生成正文固定为占位文本 | 无法正确重新编辑已整理项目、学习结论和实践记录 |
| 文件生成 | `curation.js` 将 related 固定为 []，最多截取 5 个标签 | 扩展到编辑场景会丢失关联；超限标签静默丢弃 |
| 搜索 | `filters.js` 已包含 `contentMarkdown` | 不重复开发全文关键词检索，补齐命中后的阅读能力 |
| 发布 | 公开静态站，通过 Git 提交后更新 | 文件下载与正式保存之间要有清晰反馈 |

## 3. 范围与优先级

| 编号 | 模块 | 优先级 | 交付物 |
|---|---|---|---|
| P1 | 完整 Markdown 笔记阅读 | 必做 | 详情中的完整笔记、代码块、列表和链接 |
| P2 | 收件箱完整浏览与学习页入口 | 必做 | 分页、队列搜索、选择状态恢复、查看全部 |
| P3 | 策展文件编辑与下载 | 必做 | 新建/修改表单、正文编辑、校验、预览、复制、下载 |
| P4 | 首批 30 个项目分类整理 | 内容交付 | 主分类、类型、标签、客观用途与依据，按用户授权直接导入 |
| P5 | 回归、发布和操作说明 | 必做 | 验收记录、更新站点、实际整理操作指南 |

本版沿用方案 E 的纸白、近黑、单一洋红、编辑分栏和 Phosphor 图标。增加功能时复用现有页面，不重新进行视觉方向探索。

## 4. P1：完整笔记阅读

### 用户行为

- 详情继续显示仓库事实、一句话价值、标签和关联项目。
- 有正文时增加“学习笔记”区域，完整渲染 `contentMarkdown`。
- 正文已有“学习结论”时，不在正文前再重复展示同一条结论；首页和列表可以继续使用 takeaway 摘要。
- 没有正文时显示明确空态和“整理此项目”入口。
- 支持标题、段落、强调、链接、有序/无序列表、引用、行内代码和围栏代码块。
- 不支持的扩展语法保持可读文本；本版不承诺表格、数学公式、图表、远程图片或附件展示。
- 长链接和代码块只在所属区域换行或横向滚动，手机页面整体不横向溢出。

### 实现决策

- 新建 `components/MarkdownContent.jsx`，详情页只传入正文。
- 允许为该需求引入 `react-markdown`：现有 React/Vite 没有 Markdown 解析能力，手写解析器无法可靠处理嵌套语法，依赖有当前需求依据。实施时确认 React 19 兼容版本并提交 lockfile。
- 不启用原始 HTML，不使用 `dangerouslySetInnerHTML` 或 `rehype-raw`。
- 外链允许 http/https；锚点只用于页内跳转，并明确处理标题 id。相对文件链接和其他协议不变成可执行链接。
- 新窗口外链设置适当的 rel；远程图片本版显示文字说明，不自动加载。
- 现有 Python 校验仍是发布前的最终门槛，不能把渲染器默认行为当作内容校验的替代。

### 完成标准

- [x] 一份包含多级标题、中文段落、嵌套列表、引用、代码和链接的测试笔记完整可读。
- [x] 搜索正文中的唯一词能找到项目，打开详情能看到对应文本。
- [x] 原始 HTML 和危险协议不会执行；代码块中的 HTML 作为代码显示。
- [x] 手机 390px 宽和桌面 1440px 宽都可阅读，无页面级横向溢出。

## 5. P2：完整浏览与选择状态

### 收件箱

- 保留“新收藏”和“历史待整理”两个队列，每页 30 条，提供上一页、下一页、当前页/总页数和总条数。
- 增加队列内关键词搜索，使用已有名称、描述、标签等搜索规则。
- URL 增加仅用于收件箱的 `queue`、`queuePage`、`queueQuery`；原有项目库 q/筛选语义保持独立。
- 切换队列或关键词后回到第一页，选择第一条匹配项；空结果时不展示上一个项目的表单。
- 点击条目时写入稳定 repo_id；刷新、前进和后退恢复同一队列、页面和项目。
- 从外部进入一个待整理项目时，根据 repo_id 定位所属队列及页码，不能因为它在第 31 条以后而错误选中第一条。
- 页码非法或超出范围时归一到有效范围；当前项目不在匹配集时明确取消选择或选择该页第一条，不让 URL 与表单项目不一致。

### 学习页

- 为待学习、最近已学习和仅参考提供数量及“查看全部”入口，跳转到对应阶段的项目库。
- 最近已学习按 `curatedAt` 降序排列；相同日期使用 repo_id 稳定排序。文案说明这是个人记录更新时间，不宣称是精确学习完成时间。
- 摘要区域保留最多 8 条，不另建一套学习页分页。

### 完成标准

- [x] 用至少 65 条 fixture 覆盖三页，第一、第三页条目均能选择和打开。
- [x] 直接打开第 31 条以后的项目 URL，表单显示正确项目。
- [x] 搜索、切换队列、空结果、刷新和浏览器返回时，选择与 URL 一致。
- [x] 学习页每个截断的分组都有到完整列表的入口。

## 6. P3：策展文件编辑与下载

### 页面与入口

- 增加 `view=curation&repo=<repo_id>`，详情及学习页可进入。收件箱复用同一个表单组件。
- 已整理、未整理、个人归档和 missing 项目均可按 repo_id 打开表单；missing 不导致原笔记不可访问。
- 这是“生成修改后的文件”页面，未提交 Git 前不改变线上目录或阶段计数。

### 字段与行为

| 字段 | 新建 | 修改已有文件 |
|---|---|---|
| repo_id | 来自当前项目，只读 | 保持不变 |
| 分类/类型 | 默认待分类/待判断，要求用户主动选择正式分类 | 回填原值 |
| stage | 默认 queued，用户可改 | 回填原值，支持 learned |
| note | 空值；GitHub 描述只作为参考 | 原样回填 |
| tags | 空数组 | 保留全部原值；不静默截断 |
| related | 默认空数组 | 保留现有关联，本版展示为只读 |
| Markdown 正文 | 学习结论/实践记录模板 | 回填完整 contentMarkdown，不以摘要替代 |
| updated_by_user_at | 导出有实际修改时填用户本地日期 YYYY-MM-DD | 无修改的导出保留原日期 |

- 表单提供正文 textarea 和预览，不引入富文本编辑器、自动改写或自动拆分正文。
- 支持“复制 Markdown”和“下载 <repo_id>.md”。下载内容使用 UTF-8，文件名只由数字 repo_id 生成。
- 浏览器 Blob 和 object URL 即可完成下载；不引入文件下载库。
- 提示下载文件放入 `content/repos/`，审阅差异并提交后才生效；“已下载”不能写成“已保存到项目库”。
- 文件选择、链接和元数据不能在导出时丢失。保留语义与正文内容；TOML 空白/注释不承诺字节级保留，操作指南明确要求审阅 diff。
- 若现有文件出现生成器不支持的元数据，禁止默默丢掉该字段后生成替代文件；提示回到 Git 文件编辑。

### 校验与草稿边界

- note 必填；标签最多 5 个，不允许重复或重复语言；分类、类型、阶段来自现有配置。
- learned 必须包含真实、非占位学习结论，前端提示与 Python 发布校验保持一致。
- TOML 序列化必须正确处理引号、反斜线、换行和控制字符，不能仅假定 JSON 字符串始终等于合法 TOML。
- 无效输入显示字段错误，不导出文件；超限标签要求修改，不静默删除。
- 未导出的修改在切换项目、内部导航或刷新时有一次离开确认；取消后保留当前草稿和选择。导出成功后清除该提醒状态。
- 草稿只在内存中存在，不新增 localStorage/数据库/远端草稿同步；浏览器被强制关闭时不能保证恢复。

### 完成标准

- [x] 新建文件下载后能被 Python tomllib 解析，通过 `verify_data.py` 并进入构建目录。
- [x] 修改已有文件的一个标签后，正文、related、repo_id 和其他字段仍保持原语义。
- [x] 有 related、有多段正文、个人 archived、missing 项目都可生成修改文件。
- [x] learned 的有效与无效结论、重复/超限标签、特殊字符都有验证证据。
- [x] 离开未导出草稿时可以取消，取消后内容不丢失。
- [x] 下载文件在隔离内容目录中校验并重新构建后，页面显示新分类、阶段及完整正文；正式个人内容的提交待用户确认。

## 7. P4：首批 30 个项目的内容整理

此模块是一次内容任务，不开发常驻 AI 分类服务。用户已明确授权助手自行归纳，不要求逐项确认主分类、类型、标签或客观用途。

1. 从未整理的 active 项目选择最近收藏 15 个、历史 Web/工程 10 个及其他领域 5 个，按 repo_id 去重。
2. 依据公开仓库描述、Topics 和必要的官方 README 确定用途分类，并在 `docs/curation-batch-01.md` 留下结果和依据。
3. 直接写入 `content/repos/<repo_id>.md`；note 为客观用途说明，正文标明整理依据。
4. 新增项目保留 `stage=imported`，学习结论与实践记录为空，不代写个人经历。
5. 原有 6 份记录保留原文，避免本次归类影响已有学习记录。

### 完成标准

- [x] 30 个不同 repo_id 的分类、类型、标签和可追溯依据完成。
- [x] 建立 30 份正式策展文件，累计 36 份；原有 6 份文件字节不变。
- [x] 新增条目的学习阶段保持 imported，学习结论与实践记录为空。
- [x] 导入后可按分类检索，二次同步不覆盖策展文件。

此版本范围以用户最新授权为准，替代早先“逐项确认后导入”的流程。

## 8. 文件职责与实施顺序

以下为目标路径，实施时允许依现有风格调整命名，不为计划目录而创建空模块。

| 路径 | 职责 |
|---|---|
| `app/src/components/MarkdownContent.jsx` | 安全显示完整 Markdown |
| `app/src/components/CurationForm.jsx` | 表单、预览、错误提示与导出交互 |
| `app/src/views/CurationView.jsx` | 按 repo_id 加载任意项目并挂载表单 |
| `app/src/domain/curation.js` | 回填、校验、字段保留和 TOML/Markdown 生成 |
| `app/src/domain/inbox.js` | 队列筛选、分页和定位选择 |
| `app/src/domain/routing.js` | 新视图与收件箱参数的 URL 读写 |
| `app/src/views/InboxView.jsx` | 队列与表单接线 |
| `app/src/views/DetailView.jsx`、`LearningView.jsx` | 笔记区和完整列表/编辑入口 |
| `app/src/App.jsx` | 路由接线和必要的离开草稿确认接线 |
| `scripts/star_vault.py` | 仅在保留完整可编辑元数据时补充目录输出，不改同步职责 |
| `docs/curation-batch-01.md` | 首批分类结果、依据与导入状态 |
| `docs/07-curation-guide.md` | 下载、放置、差异审阅、校验及发布说明 |

独立关注点超过约 80 行或具有复杂状态时拆模块；不把所有功能堆进 App 或 InboxView，也不建立通用表单/状态框架。

| 阶段 | 工作 | 依赖 | 参考投入 |
|---|---|---|---|
| S0 | 核对代码/数据、准备本地 fixture、跑现有验证 | 无 | 0.5 天 |
| S1 | P1 完整笔记阅读 | S0 | 0.5～1 天 |
| S2 | P2 分页、选择与学习页入口 | S0 | 1 天 |
| S3 | P3 文件编辑、下载和内容保留 | S1、S2 | 1～2 天 |
| S4 | P4 分类归纳及正式导入 | S0，正式导入依赖 S3 | 0.5～1 天 |
| S5 | P5 回归、文档、发布 | S1～S3；S4 报告实际状态 | 0.5～1 天 |

参考投入用于拆分批次，不是时间承诺。默认按顺序实施，每批完成后执行相关验证再进入下一批。

## 9. 验证与发布

### 必要测试

- Node 纯函数测试：分页边界、队列选择、URL 恢复、表单回填、字段保留、标签错误和日期逻辑。
- Python/前端交叉验证：把前端实际生成的文件写入临时测试目录，用真实解析和校验函数读取；覆盖特殊字符与 related/正文保留，不能只测试拼接字符串相等。
- 浏览器验收：Markdown 显示、安全链接、实际下载文件、草稿离开确认、手机长代码块、前三页选择及前进/后退。
- 延续现有测试工具，不为本版创建新的 UI 测试框架。只有涉及真实行为的安全与数据完整性路径才增加必要测试。

### 发布前命令

从项目根目录执行；测试新增后更新 npm test 的文件列表，避免新增用例没有被 CI 执行。

```bash
python3 -m unittest discover -s tests -p 'test_*.py'
python3 scripts/verify_data.py
python3 scripts/build_catalog.py
python3 scripts/generate_readme.py
npm --prefix app test
GITHUB_PAGES=true npm --prefix app run build
npm --prefix app run test:sites
git diff --check
```

### 发布门槛

- [x] 新建 → 下载 → 文件校验 → 构建 → 页面可见的完整旅程通过。
- [x] 编辑已有记录没有丢失正文与关联。
- [x] 方案 E 桌面/移动端截图检查通过，结果写入独立 v1.1 QA 记录，保留 v1 证据。
- [x] fixture/草案不混入正式目录，私人判断未经确认不自动发布。
- [x] diff 只包含本版范围；内容与功能代码分别审阅。
- [x] 推送后 Actions 成功，线上 build-meta 指向预期提交，Pages 子路径资源和新入口可用。
- [x] 最终报告区分本地验证、线上验证和需用户确认的内容；不批量勾选没有证据的验收项。

发布失败时保留原 Pages；如果已发布出现本版回归，revert 对应功能提交并重新运行现有流水线，不清空数据、不覆盖后来提交的笔记。

## 10. 风险与我没有防什么

| 必要机制 | 坏什么 | 频率 | 代价/处理 |
|---|---|---|---|
| 编辑时完整保留正文和关联 | 下载文件覆盖原文件后丢失学习内容 | 频率未知，编辑已有内容即可触发 | A：字段保留及跨语言往返测试 |
| 未导出草稿离开确认 | 切换项目导致已输入笔记丢失 | 频率未知 | A：一个 dirty 标记和一次确认，不增加自动保存系统 |
| 导出前格式与 learned 校验 | 下载文件无法发布或学习状态失真 | 频率未知 | A：字段提示加发布前校验，不增加重试机制 |

Markdown HTML 与危险链接处理属于本版触达解释上下文的安全基线。

明确不处理：

- 草稿跨设备同步、崩溃或强制退出后的恢复；使用内存草稿，提醒及时下载。
- 多设备同时编辑的自动合并；仍由 Git diff/冲突处理承担。
- Markdown 任意 HTML、数学公式、附件和图片托管；先覆盖文本学习笔记。
- 批量修改及一键导入全部 278 个项目；先完成可审阅的 30 个草案。
- AI 自动分类服务、在线写 GitHub API、登录后台、私有笔记、数据库、语义搜索、Release 追踪、复习提醒及自动行为统计；均不属于本版。
- GitHub Actions 调度延迟和临时网络失败的自动重试；沿用现有失败策略。

两周观察继续按 `docs/06-operations-and-observation.md` 记录；v1.1 上线时新增日期和基线记录，避免把新版改进与旧版使用数据混算。观察结果决定后续是否启动 AI 建议或在线编辑，当前不预先承诺。

## 11. 开发执行入口

后续执行本计划时，先读本文件、项目 `AGENTS.md` 和当前 git status。首先完成 S0，再实施 S1；不得把文档中的待办当作已经实现。

每批报告：完成项、变更文件、实际验证结果和未处理场景。修改计划范围时写明依据；分类与客观说明已获授权，可直接整理导入；个人学习经历不作推断。

## 12. 执行记录（2026-09-05）

- 本地基线与新增功能通过 Python 17 项、Node 19 项、Sites 4 项测试和 GitHub Pages 子路径构建。
- 浏览器覆盖真实下载、复制、草稿取消离开、后退和刷新、队列分页、完整正文与安全渲染，详见 [v1.1 验收记录](app/design-qa-v1.1.md)。
- 首次发布时仅准备 30 个分类草案；随后用户授权助手自行归纳，现已形成 [30 个正式分类结果](docs/curation-batch-01.md)，原有 6 份个人文件未改动。
- 新增 react-markdown 10.1.0，用于标准 Markdown 解析；未增加持久化、搜索、状态管理或下载依赖。
- 原有 Sites 打包保留，正式发布按本计划继续采用 GitHub Pages。
- 首次发布运行 [#33915443138](https://github.com/jiapeiyang/github-star-vault/actions/runs/33915443138) 成功，线上下载文件和第三页队列已验收。

- 分类正式导入验证：30 条均可按分类和名称检索，学习结论为空；真实三页同步写入隔离快照后，36 份策展文件哈希全部不变。Python 17 项、Node 19 项、Sites 4 项及生产构建通过。

## 13. 全量分类补充任务（2026-09-05）

用户进一步要求完成剩余 248 个项目分类。现已逐项补齐主分类、资源类型、用途标签与中文说明，新增文件统一保留 imported 阶段和空学习记录。总分类覆盖为 284/284，历史 missing 测试仓库不纳入当前覆盖率。

结果见 [第二批分类清单](docs/curation-batch-02.md)，原有 36 份文件哈希未变。该内容任务不新增自动分类服务、数据库或网站写入接口。
