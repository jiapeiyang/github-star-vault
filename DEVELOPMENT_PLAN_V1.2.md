# Star Vault 优化与迭代计划：收藏回顾与仓库解读

> 制定日期：2026-09-07。
> 状态：v1.2A / v1.2B 已完成本地开发与验证；v1.2C 已验收冻结的全部 287 项，其中 284 篇用途、使用或资料导航解读（含 GSD 迁移导览），3 篇资料受限现状说明，待处理 0。发布使用既有 GitHub Pages 流水线，线上结果独立核实。
> 交付与验证范围见 [开发交付记录](docs/09-v1.2-delivery.md)。
> 最新决定：学习阶段、学习笔记、实践记录和学习完成统计在实施时彻底删除，不保留折叠内容、次要入口或旧功能兼容层。
> 本计划替代 [上一版评估](docs/08-project-review-and-iteration-plan.md) 的优化排期；其中已验证的检索、重访等问题仍作为输入。
> 技术与视觉基线继续遵循 `AGENTS.md`；v1/v1.1 文档保留为已有实现的历史说明。

## 1. 产品定位与完成目标

**这里是用户的历史收藏资料库：帮助用户回忆自己关注过什么，找到需要的项目，并快速理解它能做什么、如何使用。**

用户主要来浏览和检索。学习工作台、学习阶段、学习笔记、实践记录及学习完成统计全部纳入删除范围，包括页面、字段、内容、校验和派生统计；不是隐藏或降低优先级。

核心使用过程：

```text
在 GitHub 收藏
→ 同步到资料库
→ 按收藏时间、领域、用途回顾，或输入记得的关键词
→ 打开仓库解读：是什么、能做什么、怎么用、看一个示例
→ 根据需要前往上游仓库、文档或在线演示
```

优化完成后应满足：

1. 不记得仓库名时，能通过用途、技术词、标签或收藏年份找回。
2. 打开一个已完成解读的条目，不必立刻去读完整 README，就能判断它是否符合当前需要。
3. 使用步骤有入口、前提、操作和预期结果；示例的来源与是否实测明确。
4. 历史收藏有时间脉络；已退出公开 Stars 的已收录记录仍能被找到。
5. 产品不再包含学习阶段、学习笔记或学习完成统计，所有浏览流程围绕收藏与仓库资料展开。

## 2. 相比上一版计划的调整

| 上一版建议 | 本次决定 | 原因 |
|---|---|---|
| 加强加入待学、开始学习、记录结论 | 删除相关功能、数据和入口 | 用户明确不需要保留 |
| 将已分类 imported 呈现为待选择学习计划 | 取消 | 已收藏、已分类的仓库无需再承担一个学习待办 |
| 学习页作为核心工作台 | 删除学习页，由收藏回顾承担新的浏览需求 | 不保留旧学习页面或次要入口 |
| 学习笔记作为详情正文主体 | 删除旧笔记内容，正文用于仓库解读与使用指南 | 不保留「原有备注」折叠区 |
| 观察笔记编辑、学习转化、学习完成数 | 删除相关统计与约束，改为找回效果、内容帮助程度和介绍覆盖 | 衡量用户实际在这里做的事情 |
| 多词搜索、标签选择、单项清筛选、返回原位置 | 保留并提升 | 是检索与连续浏览的基础 |
| 修复随机重访、改善首页精选 | 保留，改以收藏记录为依据 | 帮助发现以前收藏过的项目 |
| 学习路径、实践任务专题 | 改为可选用途专题与相似项目对照 | 帮用户选工具、找资料，不形成课程和打卡任务 |
| 在线编辑、草稿恢复 | 暂不投入 | 核心用户以只读浏览为主 |

## 3. 页面与信息架构

### 3.1 主导航

建议主导航为 **首页 / 项目库 / 收藏回顾 / 关于**。

分阶段交付时，v1.2A 完成学习模块与数据清理，内容维护只负责仓库资料；v1.2B 完成回顾页面后才加入「收藏回顾」导航，不上线占位页面或不可用按钮。

- 「新收藏」保留在首页和项目库快捷入口中；原收件箱与策展表单转到次要的「内容维护」入口。
- 删除学习页面、路由分支及所有跳转入口。旧 `view=learning` 地址按失效视图回到项目库并规范化 URL，不保留旧学习界面；仓库详情与资料维护地址继续可用。
- v1.2A 内完成相关组件、配置、数据字段和测试依赖的清理，不能只移除导航后留下运行中的旧功能。

### 3.2 首页：我最近和以前关注过什么

保留方案 E 的纸白、近黑、洋红、编辑分栏和真实头像，调整栏目内容与入口。

- 顶部搜索保持醒目，支持项目名、用途、技术词和标签。
- 核心栏目：最近收藏、领域分类、按年份回顾、随机重访、最近补充解读。
- 删除「继续学习」「最近学习记录」「待学习数量」及对应数据计算。
- 「当前关注」改为确有依据的「最近收藏」或「编辑精选」，不把已分类推断为用户当前关注。
- 精选可以复用少量 Git 配置中的 repo_id，避免在组件中固定某一个仓库；没有维护精选的需求时直接使用最近补充解读。
- 随机重访修复日期下标限制，提供「换一个」，全部符合筛选的候选均可被选中。主要面向较早收藏，范围文案与候选规则保持一致。
- 显示最近成功同步到时分；6 小时是计划检查周期，不是准点承诺。

### 3.3 项目库：高效找回

- 列表保持简短摘要，补充介绍后不把长正文塞进每一行。
- 主要筛选：领域、资源类型、策展标签、语言、收藏年份/时间范围。
- 删除学习阶段筛选与路由字段；旧链接中的 `stage` 参数丢弃并从 URL 清除，不再过滤结果。
- 上游归档、个人归档、公开 Stars 状态保留为次要筛选，避免混为一个状态。
- 项目库默认仍显示当前 Stars；明确提供「全部已收录记录」入口，不把 missing 记录丢掉。
- 支持简单多词同时匹配、大小写归一，搜索覆盖仓库解读里的用途、步骤和示例说明。
- 项目库内回车保留已有筛选；条件可单项移除；进入详情再返回时恢复筛选和原条目位置。
- 标签支持输入搜索与常用标签入口，逐批人工整理近义标签；不因为标签只出现一次就删除。
- 搜索结果补充命中片段，尤其是只命中长介绍时，显示相关句子而不是始终重复一句话摘要。

### 3.4 收藏回顾：按时间重新翻阅

以真实 `starred_at` 为收藏日期，提供年份入口和按年/月分组的时间列表；第一版不做复杂图表或兴趣分析。

- 年份入口能直接看到该时期收藏的项目，并与分类、用途搜索组合。
- 回顾页默认包含当前 Stars 和已收录的 missing 记录；个人归档仍可显式展开。入口说明其范围。
- missing 显示「已退出公开 Stars」，不武断称为「你已取消收藏」，因为私有化、删除等也可能导致退出公开列表。
- 新收藏、历史收藏、上游归档是可查询事实，不再用「未学习」「历史待整理」评价用户的收藏行为。
- 第一版的「收藏时间」表示同步数据中保留的当前/最近一次 Star 日期。重新 Star 可能改变该日期；不伪造完整 Star/Unstar 事件历史。
- 历史范围只覆盖首次建库时能读取的 Stars 与后续同步捕获的记录；无法恢复建库前已经退出列表的仓库，也不声称记录了两次同步之间的所有变化。

### 3.5 仓库详情：快速理解 + 按需深入

建议页面顺序如下：

1. **身份与摘要**：仓库名、领域/类型、一句话用途、GitHub/官方文档/演示等主要入口。
2. **仓库解读**：它是什么、解决什么问题、核心工作方式与适用场景。
3. **怎么使用**：前提、最短操作路径和必要配置。
4. **一个具体示例**：输入或目标、操作/代码、预期结果，以及来源和验证状态。
5. **注意事项与关联项目**：已知限制、维护状态、在什么需求下考虑其他已收藏仓库。
6. **来源和时间**：资料链接、内容核查日期、适用版本；GitHub 同步时间与内容核查时间分开。

原「为什么收藏」中的客观用途整理为仓库摘要；删除学习笔记、学习结论、个人实践记录及其编辑入口，不新增「原有备注」折叠区。资料来源、用法示例和示例验证记录属于仓库解读，可正常呈现。

长正文提供锚点目录，代码块支持复制和局部横向滚动。无解读条目展示已有摘要与上游入口，并准确说明「详细解读尚未补充」，不能渲染一排空标题。

## 4. 仓库解读的内容标准

### 4.1 每篇应回答的六个问题

| 问题 | 需要的内容 | 避免的写法 |
|---|---|---|
| 它是什么 | 产品形态、主要能力、解决的问题 | 只翻译 GitHub 的一句描述 |
| 应该怎样理解它 | 核心工作方式、主要组成或使用流程；与相似概念的区别 | 没有依据地推演内部架构 |
| 什么情况下有用 | 2～3 个具体使用场景，必要时指出不适用范围 | 「强大、高效、值得学习」一类空泛评价 |
| 如何开始 | 环境/平台/账号等真实前提、官方入口、3～6 个关键步骤 | 所有仓库都写 clone、install、start |
| 看一个例子 | 一个最小且有意义的任务，输入→操作→预期输出 | 只有命令，没有目标和结果；虚构 API |
| 用前需要知道什么 | 与使用相关的限制、版本、维护状态和资料来源 | 对上游质量、授权或运行成功作笼统保证 |

普通工具或库可以参考 600～1,000 字，复杂项目可更长，简单合集可以更短。字数只是阅读密度参考，不作为交付打卡；核心标准是理解和操作所需的信息足够且能追溯。

### 4.2 不同资源类型采用不同说明

| 类型 | 使用说明重点 | 示例形态 |
|---|---|---|
| 应用/工具 | 支持平台、获取入口、打开或配置、完成一个任务 | 从准备输入到看到结果的操作过程 |
| 库/框架 | 安装版本、最小接入、关键 API、使用边界 | 少量可理解的代码、输入与预期输出 |
| 模板/脚手架 | 生成或获取、依赖、环境变量、运行、主要修改位置 | 从模板改出一个最小功能 |
| 教程/书籍/课程 | 覆盖主题、适合的基础、阅读入口、目录定位 | 一个章节主题、练习或查询路径 |
| 资源合集/数据集 | 收录范围、分类方式、寻找条目或获取数据的方法 | 按一个需求找到资源；数据集可用一条样本说明结构 |
| 研究/示例/实验 | 研究问题、已有实现、复现前提与限制 | 官方示例流程或结果解释，明确未复现部分 |

教程、合集的使用示例可以是导航或选取路径，不必出现代码。需要 GPU、真实账号、付费服务或部署的示例优先依据官方资料说明，不为了补正文就执行这些操作。

### 4.3 来源和示例验证

- 资料顺序：README → 官方文档/Quick Start → 官方 examples 或相关少量源码 → 必要的 Release/归档说明。只读与目标相关的部分。
- 事实和解释要对应真实来源；安装命令、API 名称、环境变量和版本要求逐项核对。关键步骤就近链接，末尾汇总来源。
- 示例分别标为「官方示例摘录/改写，未执行」「说明性示例，未执行」或「本地已验证」，不能把静态查阅写成实测。
- 「本地已验证」必须附环境、版本、执行方式和实际观察；这不等于用户个人实践经历。
- 非代码资源说明找到了哪些真实入口，不编造本地运行记录。
- 没有核实到的用法明确标注缺口并保留上游入口。不能为了让每篇看起来齐全而拼凑内容。
- 对已归档或明显历史版本的仓库，说明历史用途与适用版本；不把旧示例包装成当前推荐方案。
- 默认不大段复制 README；用中文整理理解与使用路径，保留必要引用和短示例。

### 4.4 正文模板

以下是内容骨架，实施时按资源类型调整；不是已经完成的某个仓库介绍。

```markdown
## 它是什么
具体解释仓库的形态、能力和核心工作方式。

## 适合什么场景
列出真实任务，并说明选择它的理由或边界。

## 如何开始
前提、官方入口和完成最短路径的步骤。

## 一个使用示例
目标与输入 → 操作或代码 → 预期结果。
说明来源、适用版本，以及本例是否实际执行。

## 注意事项与相关项目
有依据的限制和关联；不存在可靠对照时不强行补足。

## 资料来源
链接与核查日期；不要把元数据同步日期当成正文更新日期。
```

## 5. 内容存储与学习模块删除方案

### 5.1 直接沿用现有仓库资料文件

旧方案新增 `content/guides/` 的原因是隔离并保留旧笔记。既然学习笔记也要删除，本计划取消该目录，直接沿用 **`content/repos/<repo_id>.md`** 的 TOML frontmatter + Markdown 保存分类、摘要、仓库解读、使用步骤与示例，一仓库一份资料。

- `data/repositories.json` 继续仅由同步脚本维护 GitHub 事实，不因删除学习模块丢掉收藏记录。
- 保留 repo_id、分类、资源类型、策展标签、客观短摘要、公开资料来源和关联仓库。
- 删除旧正文中的「学习结论」「实践记录」及个人笔记内容，包括空章节和占位文字；当前 Stars 与 missing 条目的现有文件都在清理范围内。
- 现有 `note` 中的客观用途迁移为 `summary`；需要改写的个人判断按公开资料重新整理，不把个人笔记挪个字段继续保留。
- 正文直接用于本计划的仓库解读。保留已有独立的公开资料整理依据；缺少详细解读的条目仍可展示摘要和上游入口，待后续批次补齐。
- 补充来源与实际核查日期，不把旧个人更新时间直接当成新的资料核查日期。原有有效内容更新时间可按中性含义迁移。
- 内容由人工或经授权的助手逐仓核对维护，不由定时任务自动生成；不另建数据库、内容服务或 CMS。

### 5.2 必须一起删除的内容

| 范围 | 删除项 | 完成要求 |
|---|---|---|
| 页面与导航 | 学习工作台、学习动作、笔记阅读/编辑区、学习统计卡片 | 不保留折叠区、隐藏页面或次要入口 |
| 内容与 Schema | `stage`、学习结论/实践笔记、`takeaway` 及专属元数据 | 源文件、解析器和导出文件使用清理后的契约，不保留空字段兼容 |
| 配置与路由 | `config/stages.json`、学习视图分支、`stage` 筛选参数与相关默认值 | 旧参数不影响结果，失效学习地址落到项目库 |
| 计算与产物 | 学习中/已学习/待学习计数、最近学习排序、相关 catalog 字段和 README 统计 | 新构建的 JSON、页面和 README 不再暴露学习数据 |
| 校验与测试 | learned 必须有结论等约束、仅服务已删除功能的测试 | 删除旧约束；保留通用 Markdown、资料导出、分类和链接测试 |
| 文档与观察 | 当前操作指南中的写笔记/改阶段流程和学习完成指标 | 改为仓库资料维护；已明确标注的历史验收记录不充当新需求 |

删除的是用户学习系统。`resource_type=learning` 表示教程、书籍或课程，是仓库资源类型，继续保留；上游归档、公开 Stars 状态与收藏日期也继续保留。

旧 `stage=archived` 还承载个人归档，需要在删除 stage 前迁移为独立的 `personal_archived` 布尔值（缺省 false），保持已有隐藏规则。其余学习阶段直接移除，不改成另一套学习状态。

### 5.3 读取、搜索与维护

- 更新现有资料解析、目录生成、表单回填和导出为一致的新契约，复用标准库 TOML 和 Markdown 渲染能力。
- catalog 只输出仓库事实、分类摘要、解读正文、来源和内容时间等有效信息，不再生成旧学习字段。
- 搜索使用摘要、用途、正文与示例；删除笔记和 takeaway 搜索路径，避免已删除的文字继续出现在检索结果里。
- 内容维护入口只负责分类、摘要、仓库资料与示例，移除阶段选择、笔记模板和学习完成校验；不开发在线保存或专属笔记编辑器。
- 新收藏/历史导入的来源差异从已有事实字段派生；待分类由分类缺失判断，不再用 imported/inbox 学习阶段组织队列。
- 「待补介绍」按解读内容与批次验收结果判断，不以创建文件数量代替完成。它是内容维护状态，不是用户学习任务。
- 第一版沿用静态 catalog；全文内容进入后测量实际体积、检索与渲染响应，再决定是否需要拆分加载。
- 正文长链接、代码复制和局部横向滚动可用；不为未出现的 Markdown 需求增加依赖。

### 5.4 删除实施顺序与检查

1. 盘点学习字段、正文、组件和派生统计的全部消费者，同时确认需要保留的分类、摘要、关联和个人归档。
2. 在同一开发批次中完成源内容清理、stage 移除、必要的字段迁移，以及读取/展示/导出契约修改；不发布半迁移状态。
3. 删除失去用途的组件、样式、配置和纯学习测试，更新仍有用途的通用资料组件与测试。
4. 重新构建 catalog 和 README，检查删除内容不再通过 JSON、搜索、详情、导出或首页出现。检查前端构建目录无过期学习模块残留。
5. 对比迁移前后的仓库 ID、数量、分类、标签、关联和个人归档结果，确认删除范围没有扩散到收藏资料。

不为被删除的学习内容新增备份目录、归档页面或隐藏兼容字段；本计划不要求改写已有 Git 提交历史。

## 6. 内容建设计划：先确定质量，再覆盖全部

新要求包含内容建设，不能只修改页面布局或多展示几段占位文字。工程功能与真实解读内容分别验收。

### 第一批：12 个代表仓库

按 6 种资源类型各选择约 2 个，兼顾近期与较早收藏、活跃与归档仓库；优先选择官方资料完整、能检验不同模板的项目。

- 在实施开始时从最新快照冻结 repo_id 清单，避免使用已过时的 Stars 总数。
- 每个条目真实查阅公开资料，完成解读、用法、示例和来源；一两句旧摘要不能计作新解读。
- 完成桌面/手机阅读与搜索验收，形成样例和写作标准。实施获批后可自主进行公开内容整理，无需逐条批准。
- 这 12 个是首批交付，不是全部仓库已优化；只有用户提出方向性调整时才重定标准，不为每批例行阻塞确认。

### 后续批次：覆盖实施时冻结的全部当前 Stars

每批约 20～30 个，按以下顺序推进：

1. 新收藏、近期收藏以及高频使用领域中的条目。
2. 用户常见需求对应的应用、开发工具、库和框架。
3. 历史教程、资源合集与归档项目。

顺序由收藏时间、分类和明确反馈确定，不伪称掌握用户的点击热度。默认目标是冻结范围内所有当前 Stars 都有可理解的解读；已收录 missing 条目同样清除旧学习内容，保留收藏事实与客观资料，详细解读按后续需要补充。

批次清单分别记录「已完成」「资料不足」「待处理」和具体原因，记录路径、来源日期与示例验证方式。新增 Star 单独进入下一批，避免移动分母掩盖当前批次完成度。

资料不足时至少说明能确认的用途、可访问的资料入口和缺口，不将无依据的占位介绍计为完整交付。报告必须单独列出受限条目，不能以创建文件代替完成用法与示例。

### 持续维护

每 6 小时同步只更新 GitHub 事实，不重写长介绍，也不自动把内容核查日期刷新成当天。新增仓库或用户再次关注某条目时按需查证；批次维护时优先复核示例已明确失效或上游发生相关重大变化的条目。不建设全量 README 定时抓取或后台自动摘要任务。

## 7. 迭代顺序与交付边界

| 阶段 | 核心交付 | 验收停止点 |
|---|---|---|
| **v1.2A：删除学习模块并升级仓库解读** | 学习页面/字段/笔记/统计彻底删除；现有资料文件升级、详情阅读、12 个真实样例；多词搜索与随机重访修复 | 删除验收通过；收藏事实与客观资料保留；用户可理解样例用途、使用步骤及有来源的示例 |
| **v1.2B：收藏回顾与检索体验** | 年份/时间范围筛选、年/月回顾、历史记录入口、标签搜索、单项清筛选、返回原位置、命中片段 | 能按“哪一年、什么用途”找回仓库，连续打开多个详情后仍能接着浏览 |
| **v1.2C：全量解读建设** | 按 20～30 个一批完成冻结范围的仓库解读，维护受限条目清单 | 冻结清单每项均有验收结果；完整覆盖与资料不足分别报告，不能以首批样例代替全量交付 |
| **v1.3：有使用需求后再加** | 用途专题、相似项目对照、旧资料重点复核；必要的检索或加载优化 | 解决实际找回/选用问题，不增加学习打卡、账号或笔记流程 |

依赖关系：v1.2A 的内容标准和阅读链路确认可用后，v1.2B 与后续内容批次可交错推进。每一批工程与内容均独立验证；公开发布须在对应发布授权范围内执行。

工程部分可先按约 4～7 个有效开发日预估；12 个样例的内容工时单独记录。全量整理的工作量在样例完成后按资料难度重新估算，不把数百仓库的资料核查隐含在几天前端开发里，也不预先保证所有示例实测。

本轮已按上述顺序完成 v1.2A / v1.2B，并验收 v1.2C 的冻结范围。后续新增收藏与 v1.3 按实际需求推进；资料受限项保持明确边界，不伪造缺失的用法。

## 8. 具体验收标准

### 内容

- 首批 12 个有真实选择清单，覆盖不同类型与时间段；每篇回答本计划的核心问题，提供可操作或可导航的示例。
- 命令、API、路径和版本有来源；未执行的示例明确标注，实测记录有证据。
- 归档、缺少维护、来源不足等情况具体说明，不一概标成「活跃」「可直接运行」。
- 旧学习笔记、结论、实践记录和 stage 已删除；仓库 ID、分类、标签、关联、客观资料及个人归档结果保留。
- 校验 repo_id、来源 URL、日期与必需内容结构；内容完整性仍须人工查阅，不能只靠字数和标题数量。

### 用户主流程

- 首页、项目库、详情和维护入口均不存在学习阶段选择、笔记填写或学习完成统计。
- 长正文有清晰层级、锚点和代码复制；没有解读时仍可使用官方入口，没有空学习章节占位。
- `Claude MCP` 等冻结用例验证多词检索；新增介绍中的独有用途词可被找到，打开详情可看到命中内容。
- 筛选后的回车、详情返回、浏览器前进后退保持语义与位置一致；返回上下文中的 missing 和归档筛选不丢失。
- 收藏回顾按真实日期与指定时区分组；有效详情链接保持可用，旧学习地址和 stage 参数不再启用旧功能，空年份结果有明确处理。
- 不同来源状态能区分；同一仓库更名不丢解读，退出公开 Stars 不删除收藏记录，重新 Star 不承诺保留系统未记录的全部历史事件。

### 工程

- 运行相关 Python 测试、`npm --prefix app run test`、`npm --prefix app run test:sites` 和 `npm --prefix app run build`。
- 增加资料读取与安全链接、学习内容删除、收藏资料保留、个人归档迁移、分词搜索、日期筛选和随机候选可达等有实际契约价值的测试。
- 验证桌面与 390px 视口的长中文、长仓库名和代码块，检查键盘、焦点与选中语义。
- 复制代码和下载新资料文件的范围明确，导出不再包含旧学习字段或笔记；普通 Markdown 不执行 HTML 或危险链接，不把示例代码自动当作待执行指令。
- 针对源文件、生成 catalog、页面、搜索、导出和 README 检查旧学习内容与统计确实消失；历史文档及删除迁移测试中的术语不作为运行功能残留。
- 完整内容批次构建后检查实际大小、检索响应和首屏/详情阅读情况，记录具体环境与结果。

## 9. 使用观察与成功指标

原 9 月 12 日、19 日观察节奏可以保留，但口径改为本计划；原来的学习完成和笔记编辑指标退出。网站不新增行为埋点 SDK，也不要求用户为了配合验证持续写日志。

| 指标 | 建议验证方式 |
|---|---|
| 找回效果 | 选 10 个“知道用途但记不清名字”的任务，用关键词/时间筛选检查是否能找到；30 秒可作参考目标，不虚报真实耗时 |
| 解读帮助程度 | 抽查已完成条目：读后能否回答是什么、适用何处、下一步怎么做 |
| 示例质量 | 对需运行的例子区分资料核对与实测；对教程/合集验证真实导航入口与示例内容 |
| 回顾效果 | 能否按某年/某月查看对应收藏，发现和打开旧项目 |
| 覆盖程度 | 按冻结清单分别报告完整解读、资料受限、待补充数量 |

不再衡量学习转化率、笔记数量、是否清空 imported，用户仅浏览也符合产品目标。

## 10. 本轮交付与“我没有防什么”

学习模块、字段与原笔记章节现已删除，沿用 content/repos 保存仓库资料。收藏回顾、检索改进与全部 287 个冻结条目已完成本地验收，其中 3 项为资料受限的现状说明；用户已于 2026-09-07 授权提交和发布；开发交付记录及全量内容验收清单保存本地验收范围，线上结果以部署记录为准。

- 本轮已通过同步脚本刷新为 287 个当前 Stars、288 条历史记录，287 个已分类且有经核查的正文；其中 284 篇用途、使用或资料导航解读，3 篇资料受限说明。覆盖明细见 `docs/content-batches/v1.2-acceptance.md`。
- 本计划不承诺恢复建库前已消失的完整收藏史，不把 missing 等同于用户取消 Star。
- 不保证第三方仓库所有命令在所有平台可运行，不以“已核对官方文档”替代实际执行证据。
- 不全量复刻 README、自动下载大模型或运行需要真实账号、付费、部署的示例；资料不足就报告边界。
- 实施时按用户要求删除已有学习内容，不提供旧笔记兼容入口；删除范围不包含收藏事实、分类、客观仓库介绍和上游使用示例。
- 原有 `app/qa/production-learning.png` 改动和上一轮评估证据保持保留。
