+++
repo_id = 74293321
category = "web-client"
resource_type = "library"
tags = ["编译器", "组件", "UI"]
related = []
summary = "把声明式组件编译成针对 DOM 更新的 JavaScript 的前端框架。"
content_updated_at = "2026-09-07"
reviewed_at = "2026-09-07"
sources = ["https://github.com/sveltejs/svelte/blob/main/README.md", "https://svelte.dev/docs/svelte/overview"]
+++

## 它是什么
Svelte 允许在 .svelte 组件中组织模板、脚本和样式，再由编译器生成用于更新 DOM 的 JavaScript。它把一部分传统运行时工作放到构建阶段，组件可以独立嵌入页面，也可结合 SvelteKit 构建带路由与服务端能力的应用。

## 适合什么场景
适合喜欢接近 HTML/CSS 的组件表达，或想比较编译式框架与 JSX 路线的人。Svelte 核心关注组件，完整应用的路由、加载和部署通常属于 SvelteKit 等上层工具，选型时应分开确认。

## 如何开始
官方 Overview 推荐新用户先打开交互教程或 Playground，无需安装。试本地工程时从 Getting started 选择应用路径，再按对应版本的 CLI 创建。当前文档将 Runes 与 Legacy APIs 分列，旧教程的事件和状态语法不应与新示例混用。

## 一个使用示例
官方 Overview 示例摘录，未执行：在组件 script 中定义 `function greet() { alert('Welcome to Svelte!'); }`，模板写 `<button onclick={greet}>click me</button>`，style 中为按钮设置字号。预期点击按钮出现欢迎提示，组件样式应用到按钮；这展示事件与局部样式，不需要先构建后台。

## 注意事项与相关项目
资料中能看到 Svelte 4、Svelte 5 的迁移入口，旧文章需要明确版本。编译成功不证明可访问性、业务状态或服务端行为正确。Solid 同样追求精细 DOM 更新，但采用 signal 与 JSX，阅读两者时应关注更新模型而不只比较语法长度。

## 资料来源
- [上游 README](https://github.com/sveltejs/svelte/blob/main/README.md)
- [补充官方资料](https://svelte.dev/docs/svelte/overview)

资料核查于 2026-09-07；以上说明基于公开资料，示例未在本站执行。
