# v1.3：10 个具体使用示例

2026-09-07，已完成 10 / 10 篇「输入 → 操作 → 输出」示例，10 / 10 均完成本地范围有限的执行验证。原有未执行示例与新增验证小节分开，没有把整仓库或用户个人实践标为已验证。

## 交付范围

独占修改：99401299 Nano ID、4297695 json2csv、11551538 Koa、790359 Sequelize、8467117 node-xlsx、115110181 SWC、227978903 VueUse、130884470 Solid、74293321 Svelte、358508682 mshared。

只新增这 10 份内容文件中的示例和相关来源，以及 `docs/examples/v1.3/` 的复验源码、独立依赖清单、实际输出与本报告。未修改应用依赖、应用锁文件、同步事实数据、配置或前端代码；未提交、推送或部署。

## 核查与实际结果

| 仓库 | 输入 | 实际结果 |
| --- | --- | --- |
| Nano ID | 标题 Cache guide；默认生成器与 10 位 hex 字母表 | 标题修改后 ID 不变；默认长 21，hex 长 10，字符断言通过 |
| json2csv | Ada, "A" / 2 和 Lin / 0 | 固定字段，逗号与双引号正确编码，0 保留，精确字符串断言通过 |
| Koa | GET /?name=Ada&count=2 | 200、JSON、Hello Ada / 24；before → handler → after；临时服务关闭 |
| Sequelize | User.build({ name: 'Ada' }) | 默认 green、生效后改 blue、id null、isNewRecord true；未访问数据库 |
| node-xlsx | Report 工作表，字符串/数字/布尔值 | build 得到 Buffer，parse 后与输入深度相等 |
| SWC | const price: number = 12; console.log(price * 2); | 类型标注移除，执行自写编译结果输出 24 |
| VueUse | useCounter(1, { min: 0, max: 3 }) | 初值、inc(5)、dec(10)、set(2)、reset → [1,3,0,2,1] |
| Solid | signal 值 0、2、2、3 | memo 值 0、4、4、6，计算 3 次；客户端响应式入口，无 DOM |
| Svelte | name='<Ada>'、count=2 的组件 | `<!--[--><p>Hello &lt;Ada>; total: 24</p><!--]-->`，0 编译警告 |
| mshared | main.count 0 → 2 → 3；子实例退订 | 读到主子两份状态，只收到退订前 0 → 2 的一次通知 |

全部直接版本、复验命令与限制见 [示例说明](../examples/v1.3/README.md)。实际原始输出见 [verified-output.txt](../examples/v1.3/verified-output.txt)，来源和源码 SHA 见 [sources.json](../examples/v1.3/sources.json)。

## 环境与安装边界

- macOS 26.3.1 arm64、Node.js 24.14.0、npm 11.9.0。
- 安装目录 `/tmp/star-vault-v13-examples.XZrPyY`，公开来源缓存 `/tmp/star-vault-v13-example-sources`。
- 全部实际包的 resolved URL 均来自 `https://registry.npmjs.org/`，安装使用 `--ignore-scripts --no-audit --no-fund`。传递版本与 integrity 另存 installed-packages.json；没有修改 Star Vault 的依赖和锁文件。
- node-xlsx 最新版 0.24.0 的 xlsx 依赖来自站外 CDN；为保持本批次 registry 来源边界，使用明确标注的 0.21.2 / xlsx 0.17.5 历史组合，未改写成“最新版验证”。
- mshared 0.0.4 加载 React 16.14.0 仅为旧 peer dependency；没有渲染组件。
- npm 安装报告 uuid 8.3.2 与 dottie 2.0.7 弃用警告；这些是现有 Sequelize 依赖链，本批次不修改上游依赖，也不据示例通过声称依赖无漏洞。

## 验证与清理

- `node run-all.mjs`：最终退出 0，PASS: 10 public examples；stderr 为空。
- 首次 Svelte 断言错误地要求 `>` 转成 `&gt;`，真实输出仅转义 `<`。已核查输出、保留安全文本边界并修正为该版本的精确结果，重跑全部 10 项通过；未修改上游库。
- Koa 最终临时端口 53002，server.listening=false；`lsof -nP -iTCP:53002 -sTCP:LISTEN` 无结果。首次运行端口 52784 也已检查无监听。
- Svelte 临时生成目录已清理，无 `.svelte-example-*` 残留。
- `python3 scripts/verify_data.py` 通过：active=287 total=288 curations=287。
- 10 篇的 repo_id、category、resource_type、tags、related、personal_archived、summary 与 HEAD 一致；均保持六个主章节，原例与新增验证各一个小节。
- `git diff --check`（本批次文件）通过。
- Solid API 站点返回 403，已改读官方仓库 signal.ts 的 API 注释、默认 equals 与 createMemo 实现；来源清单记录该文件 SHA，没有冒充网页读取成功。
- 应用级测试和构建由主代理整合后统一执行。

## 我没有防什么

没有连接数据库、提交模型请求、使用真实账号、读写用户工作簿、启动浏览器或更改系统配置。只验证固定小输入，未覆盖大规模性能、跨版本兼容、不可信文档、生产输入校验、依赖安全审计或所有 API。浏览器事件、存储、渲染和跨应用装载未以 Node 结果冒充通过。临时安装目录保留用于复核，当前没有运行服务。
